1. 尽管官方不强制要求通过actions进行修改，但是最好还是通过actions的方式来修改状态，否则会导致组件状态的修改难以定位；
2. 对状态修改的逻辑尽量封装在store自身，而不要将其直接写在业务组件里，道理同上；
3. 对每一个组件都用observer包裹，假如只包裹了父组件，没有包裹子组件的话，可能会导致本来只更新子组件就可以，现在需要父组件整体都更新；
4. 将UI状态和全局状态分离，
5. 尽量不要和useState混用，单个组件维护一个本地状态树是完全可以的，如果混用的话，因为更新机制的不同，可能会导致一些细微的bug出现；同时还不好排查